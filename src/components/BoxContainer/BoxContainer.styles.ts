import { center } from "@/common.styles";
import { css } from "catom";

export const box = [
  css({
    boxShadow: "var(--box-shadow)",
    maxWidth: "80vw",
    margin: "auto",
    padding: "2rem",
    borderRadius: "10px",
  }),
];
